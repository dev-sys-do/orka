version: 1                     # Default to last supported version 
workload:
    kind: container            # "container", "network" 
    name: postgres             
    networks:
    environment:
    image: postgres:15