services:
  brdige:
    container_name: brdige
    build:
      context: ..
      dockerfile: ./test/Dockerfile
    environment:
      NETCONFPATH: /opt/cni/netconfs
      CNI_PATH: /opt/cni/bin/
      CNI_COMMAND: ADD
      CNI_CONTAINERID: 1111
      CNI_IFNAME: eth0
      CNI_NETNS: /run/netns/red
    privileged: true
    # cap_add:
    #   - NET_ADMIN
    #   - SYS_ADMIN
